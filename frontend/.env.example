# Local backend port used by frontend/src/utils/api.js in development.
# Set this to the port your local backend is currently listening on.
REACT_APP_LOCAL_PORT=3000

# Optional Vite-native alternative (either one works with current code).
# VITE_LOCAL_PORT=3000
